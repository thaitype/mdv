# Milestone 2 — TODO

Batch 1 (autopilot, auto mode, started 2026-05-24)

- [x] **task-1: Path resolver + unified asset dispatcher + containment**
  Replace `src/resolve.ts` with a single resolver that maps a URL path + cwd to a disk path using the extension table: `.mjs` probes `.tsx`→`.ts`→`.jsx`→`.js` (first match wins); `.css` exact match; unknown extensions are unresolvable. Resolver also performs `..` segment rejection at URL-parse stage and a `fs.realpath` containment check after lookup; both failures collapse to a single "unresolvable" outcome with no enforcement-layer signal. Wire into `src/server/app.ts` as a single `GET /*` dispatcher route that replaces the milestone-1 `GET /<component>.mjs` and `GET /<file>.css` routes; add `GET /_*` (anything under `/_` not matching the MDX entry route) returning 404. Per status table in `_contract/http-routes.md`: all misses (file-not-found, containment violation, unknown extension, `..` in URL) return `404` + `text/plain; charset=utf-8` + body `Not found: <url>`. esbuild transform options unchanged from milestone-1. Update existing `src/resolve.test.ts` and `src/server/compile-asset.test.ts` to reflect new resolver shape; add new unit tests for `..` rejection, containment via symlink escape, and unknown-extension handling. Per `_contract/http-routes.md` §`GET /*` and §"Containment check".

- [x] **task-2: CLI strict validation + boot pipeline + MDX entry basename**
  Update `src/cli.ts` to drop the `--assets` flag entirely. Add three-step validation before any server boot in the order specified by `_contract/cli.md`: (1) extension check — `<file>` must end exactly with `.vis.mdx`, non-zero exit + stderr rename hint on failure; (2) existence/readability check; (3) containment check — entry's realpath must descend from cwd's realpath, non-zero exit + stderr error on failure. Update `src/commands/serve.ts` to drop assetsDir from its config, accept cwd-based config instead, and print to stderr (in order): `vismd: working directory: <abs cwd>` then `vismd: serving <entry-as-given>`. Stdout still prints exactly one line `http://<host>:<port>` after bind. Update `src/server/app.ts` MDX entry route: basename = entry filename with full `.vis.mdx` suffix stripped (e.g., `docs/intro.vis.mdx` → `intro`), URL `/_mdx/<basename>.mjs` must match exactly or 404. Update `src/server/shell.ts` HTML shell to use the stripped basename. Update existing tests for `compile-mdx.test.ts`, `shell.test.ts`, and any cli-touching tests to reflect new basename + new config shape. Per `_contract/cli.md` and `_contract/http-routes.md` §`GET /` + §`GET /_mdx/<basename>.mjs`.

- [x] **task-3: Fixture migration + smoke test rewrite + manual check doc**
  Rename `examples/example.mdx` → `examples/example.vis.mdx`. Rename `examples/bad-placeholder.mdx` → `examples/bad-placeholder.vis.mdx`. Move `examples/components/styles.css` → `examples/styles/main.css`. Update `example.vis.mdx` imports to real paths: `import Hello from "{{VISMD_LOCAL}}/components/Hello"` and `<link rel="stylesheet" href="{{VISMD_LOCAL}}/styles/main.css" />`. Rewrite `scripts/smoke-test.mjs` to spawn `node ../dist/cli.js example.vis.mdx --port 5173 --no-open` with `cwd: examples/` (no `--assets` flag); assert boot stderr contains a line matching `vismd: working directory: <abs path ending in /examples>`; curl the new route shape (`GET /`, `GET /_mdx/example.mjs`, `GET /components/Hello.mjs`, `GET /styles/main.css`); add a containment-violation curl (`GET /../etc/passwd` → 404 + terse body); spawn a second server on `bad-placeholder.vis.mdx` and assert 500 + `Unknown placeholder: {{VISMD_FOO}}`; spawn with a plain `.mdx` arg and assert non-zero exit + stderr rename hint; spawn with an MDX path outside `examples/` and assert non-zero exit + stderr containment error. Update `examples/MANUAL_CHECK.md` for the new invocation (`cd examples && vismd example.vis.mdx`) and the new fixture paths. Per `_goal/serve-from-cwd.md` §"Success criteria".

## Verification gate

Milestone-2 is "done" when:
- `pnpm run build` succeeds.
- `pnpm test` (unit tests) succeeds — all milestone-1 tests still pass plus new ones for the resolver + dispatcher + CLI validation.
- `pnpm smoke` succeeds — exercises the new route shape, fixture layout, and CLI validation.
- The updated `examples/MANUAL_CHECK.md` exists; running it is the human's responsibility.
